# ── Postgres ──────────────────────────────────────────────
# Postgres creds default to CRUNCH_ID in docker-compose.yml.
# Override here only if you need different values.
# POSTGRES_USER=custom-user
# POSTGRES_PASSWORD=custom-pass
# POSTGRES_DB=custom-db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ── Competition identity ─────────────────────────────────
CRUNCH_ID=starter-challenge
# CRUNCH_PUBKEY=                        # on-chain crunch address (for emissions)
NETWORK=devnet                           # devnet | mainnet-beta

# ── Model runner ─────────────────────────────────────────
MODEL_RUNNER_NODE_HOST=model-orchestrator
MODEL_RUNNER_NODE_PORT=9091
MODEL_RUNNER_TIMEOUT_SECONDS=60
MODEL_BASE_CLASSNAME=tracker.TrackerBase

# ── Feed (data source) ──────────────────────────────────
FEED_SOURCE=pyth
FEED_SUBJECTS=BTC
FEED_KIND=tick
FEED_GRANULARITY=1s
FEED_POLL_SECONDS=5
FEED_BACKFILL_MINUTES=180
FEED_CANDLES_WINDOW=120
FEED_RECORD_TTL_DAYS=90
# FEED_RETENTION_CHECK_SECONDS=3600

# ── Scoring ──────────────────────────────────────────────
SCORING_FUNCTION=starter_challenge.scoring:score_prediction

# ── API Security ────────────────────────────────────────────────────
# Set API_KEY to enable authentication on admin endpoints.
# When unset, all endpoints are open (backward compatible).
# API_KEY=change-me-to-a-strong-secret
# API_READ_AUTH=false
CHECKPOINT_INTERVAL_SECONDS=60

# ── Checkpoint / Emissions ───────────────────────────────
# CHECKPOINT_INTERVAL_SECONDS=604800    # weekly (default)
# COMPUTE_PROVIDER_PUBKEY=              # wallet pubkey for compute provider rewards
# DATA_PROVIDER_PUBKEY=                 # wallet pubkey for data provider rewards

# ── Pyth-specific ────────────────────────────────────────
PYTH_HERMES_URL=https://hermes.pyth.network
PYTH_TIMEOUT_SECONDS=5

# ── Ports (change to avoid collisions when running multiple workspaces) ──
REPORT_API_PORT=8000
REPORT_UI_PORT=3000

# ── Web UI ───────────────────────────────────────────────
REPORT_UI_APP=starter
REPORT_UI_BUILD_CONTEXT=https://github.com/crunchdao/coordinator-webapp.git
REPORT_UI_DOCKERFILE=apps/starter/Dockerfile

# ── Scheduled predictions (optional) ─────────────────────
# SCHEDULED_PREDICTION_CONFIGS_PATH=config/scheduled_predictions.json
